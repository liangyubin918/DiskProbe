# 2026-08-20

- 完成 DiskProbe 对抗式代码审查：发现特权 helper 可替换导致本地提权、root 写入用户可控路径的 TOCTOU、真实扫描首次认证状态错误、扫描停止/重启竞态等高优先级问题；构建验证受运行环境 sandbox 限制而未能执行。
- 根据审查结果移除了 AuthExec、DiskProbeRead 及 bundle 内特权启动路径，并关闭真实裸设备读取；ScanEngine 现以 run ID 隔离任务、修正块数/进度边界。修复了 SMART 与 diskutil 子进程 pipe 死锁及 SMART 溢出问题。语法检查和完整类型检查均通过；`swift build` 仍因当前 CLT 的 sandbox-exec 权限限制无法运行。
