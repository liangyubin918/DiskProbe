# Experience — 本项目踩坑提炼

> 每条规则都对应至少一次真实返工。只记录**可迁移的共性**，项目细节看 git log。
> 格式：规则 → 症状 → 根因。再 coding 时先扫一遍这个文件。

## 一、权限与特权进程（本项目返工最多的领域，授权问题前后改了四轮）

### 1. 绝不提升"用户可写路径里的可执行文件"
- **症状**：初版用 `AuthorizationExecuteWithPrivileges` 执行 bundle 内 helper，存在本地提权 + 路径竞态，整体推翻重写。
- **规则**：特权方案只用系统托管 + 签名校验的通道（SMAppService / SMJobBless）。凡是要 root 干活，先问一句"攻击者替换了这个文件会怎样"。

### 2. 系统托管的注册信息会过期，"已注册" ≠ "能运行"
- **症状**：重新打包 app 后 daemon 反复 spawn 失败（launchd `runs=307, exit=78`），但 `SMAppService.status` 依然显示 enabled，UI 一度显示"已就绪"。
- **规则**：
  - 产物变更（重打包/移动位置）后必须**重新注册**，把重装做成一键入口；
  - 注册状态只是登记信息，**必须端到端 ping 探活**才能宣称"可用"；
  - 探活顺便回传版本号，能识别"注册的是旧版 helper"。

### 3. macOS 隐私门控按进程身份，root 不豁免
- **症状**：root helper `open(/dev/rdiskN)` 返回 EPERM。
- **规则**：涉及 TCC 保护资源（裸设备、用户数据）时，把"完全磁盘访问授权"当作前置条件设计进 UI（检测 EPERM → 给直达设置页的按钮），而不是当成异常报错了事。

### 4. 一条授权链有 N 个失败点，文案必须区分；成功必须清旧错误
- **症状**：安装失败的红色提示与"已就绪"的绿色状态同时挂在界面上（旧错误从不清除）；三种失败（未安装/被拒/超时）曾共用一句含糊文案。
- **规则**：操作结果与状态展示分离；**每次新操作开始时清除旧结果**；失败文案精确到环节并附带修复指引；把完整错误（domain/code）写进诊断日志文件——用户截图永远比你想象的少。

### 5. 排查特权进程问题，先上三板斧再猜测
- 症状：helper 毫秒级崩溃，进程表都抓不到，log show 查不到隐私日志，绕了很多弯路。
- 规则：daemon plist 加 `StandardErrorPath` 留 stderr；root 崩溃直接读 `/Library/Logs/DiagnosticReports/*.ips` 崩溃栈；`launchctl print system/<label>` 看 runs/exit code。**崩溃栈一出来，猜测全部作废。**

## 二、API 误用：编译通过 ≠ 运行正确

### 6. 名字相似的 API，check-in 路径完全不同
- **症状**：`NSXPCListener.service()` 在 launchd daemon 下启动即 SIGTRAP（毫秒级崩溃，进程表难抓），所有"连接超时"症状皆源于此。
- **规则**：daemon 用 `NSXPCListener(machServiceName:)`；`.service()` 只用于 XPC Service bundle。**用错不报编译错、崩得无声无息**——凡是有"service/daemon/agent"多种角色的框架 API，先确认自己的进程类型。

### 7. KVC/NSValue 取出的 C 结构体，`as? T` 运行时恒为 nil
- **症状**：`connection.value(forKey: "auditToken") as? audit_token_t` 永远失败 → helper 拒绝包括合法 app 在内的所有客户端。
- **规则**：ObjC 桥接的字节块用 `NSValue.getValue(&t)` 拷贝，不要指望 Swift `as?` 解包 C 结构体。给校验函数的**每个失败分支加日志**，否则永远不知道死在哪一环。

### 8. 外部工具的退出码语义先查文档
- **症状**：smartctl 退出码是位掩码（bit3=健康 FAILED），按"非 0 即失败"丢弃输出——恰好漏掉最需要报告的坏盘。
- **规则**：调用任何外部 CLI 前，先确认退出码是非负成败还是位掩码/计数器；**有合法输出时优先解析输出，退出码只做补充**。

## 三、异步与并发

### 9. CheckedContinuation 必须保证"恰好 resume 一次"
- **症状**：XPC 连接被拒/未建立时回执永不来，`await begin()` 永久挂起，界面毫无反应。
- **规则**：所有出口（正常/失效/超时/取消）收敛到同一个"resume 一次"的守卫函数（锁 + 置 nil）；**每个 await 都配超时兜底**。

### 10. 把正确性放进快照，而不是事件序列
- **症状**：进度事件逐条累加统计，事件丢失/重复即错；unbounded 缓冲有内存风险；两个消费者会瓜分事件。
- **规则**：进度类数据由生产方维护**全量快照**（含地图+累计值），流用 `bufferingNewest(1)`，监听收敛为单消费者。事件可丢，状态不丢。这个重构一次性消除了三个并发 bug。

### 11. 非结构化 Task 不继承取消、不保证顺序
- **症状**：`Task.detached` 里的读取在 `.task(id:)` 取消后继续跑，陈旧结果覆盖新数据；阈值快速调整可能乱序写入。
- **规则**：需要随生命周期取消的用结构化 Task；后台任务结果落地前**校验"当前上下文还是不是它发起时的上下文"**（代数序号/归属比对）；连续快速变更用去抖收敛。

### 12. 跨线程共享的标志位，从第一天就加锁
- **症状**：XPC 回调线程与工作队列共享 `fd/stopFlag`，审查阶段才补锁——带病运行了整个开发期。
- **规则**：新增跨队列可变状态的那一刻就写锁，"先跑起来再说"的债利率很高。

## 四、产品与发布

### 13. 同一数据多处定义，必然失同步
- **症状**：发 v2.5 时 Info.plist 停在 2.4；切盘后界面残留上一块盘的数据。
- **规则**：版本号、阈值等"多处出现"的值，要么单一来源派生，要么写进发布 checklist；**界面显示与数据归属解耦**（结果绑定归属者，视图按归属条件渲染），切换上下文时不清数据、不做存档。

### 14. 发布物要在"用户环境"里验证一遍
- **症状**：`ditto` 打 zip 混入 AppleDouble 文件，普通解压后签名密封校验失败。
- **规则**：二进制发布的最后一步永远是：**下载自己的 Release → 解压 → `codesign --verify --strict` → 跑起来**。打包方式优先 `zip -qry`（避免 AppleDouble）。

### 15. 单次采样不可靠，结论需要复检
- **症状**：同一块盘两次扫描结果不同——单次读耗时受系统 IO 抖动影响，把正常块误判为慢块。
- **规则**：任何"按单次测量下结论"的逻辑（性能分级、健康判定、flaky test），对可疑样本**复检 N 次取最优/多数决**；正常样本走快路径零开销。

### 16. 系统行为不能靠脑补，免费与付费的边界要实测
- **症状**：一直以为"没付费账户签名没法用"，实际上免费 Apple Development 证书完整支撑了 SMAppService daemon（本机就是活证据）；"7 天过期"是 iOS 专属。
- **规则**：平台限制类结论，要么给出处，要么在自己机器上验证；想当然的成本是整个功能被白白搁置。
- **新例**（macOS 26）：`NSSavePanel.allowedContentTypes` 传多个类型不再显示系统"文件格式"下拉框（老版本会）。关键交互别依赖系统自动行为——单类型 + 自绘 accessory 弹出菜单在 11–26 表现一致。
